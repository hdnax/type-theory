# Simple Evaluator for Lambda Calculus with Numbers and Booleans

## Exercise Statement

Build a simple evaluator for an untyped lambda calculus extended with numbers and booleans.

## Tasks

- [x] Implement an AST for variables, λ, application, booleans, numbers.
- [x] Implement a basic evaluator (substitution-based is fine).
- [x] Extend the interpreter with `if` and numeric `+`.
